# Домашнее задание к занятию "`SQL. Часть 2`" - `Скворцов Александр`


## Задание 1

**Требования к заданию:**
Одним запросом получите информацию о магазине, в котором обслуживается более 300 покупателей, и выведите в результат следующую информацию:
1. Фамилия и имя сотрудника из этого магазина;
2. Город нахождения магазина;
3. Количество пользователей, закреплённых в этом магазине.

**Процесс выполнения:**

1. Создание учетной записи sys_temp:

```
SELECT
    s.first_name AS employee_first_name,
    s.last_name AS employee_last_name,
    ci.city AS store_city,
    COUNT(c.customer_id) AS total_customers_in_store
FROM
    customer AS c
JOIN
    store AS st ON c.store_id = st.store_id
JOIN
    staff AS s ON st.manager_staff_id = s.staff_id
JOIN
    address AS a ON st.address_id = a.address_id
JOIN
    city AS ci ON a.city_id = ci.city_id
GROUP BY
    st.store_id, s.first_name, s.last_name, ci.city
HAVING
    COUNT(c.customer_id) > 300;
```

![Список сотрудников магазина](img/5.png)

---

## Задание 2

**Требования к заданию:**
1. Получите количество фильмов, продолжительность которых больше средней продолжительности всех фильмов.

**Процесс выполнения:**

```
select COUNT(f.title)
from film f  
where f.`length`  >
  (select AVG(`length`) 
  from film);   
```

![Количество фильмов](img/6.png)

---

## Задание 3

**Требования к заданию:**
1. Получите информацию, за какой месяц была получена наибольшая сумма платежей, и добавьте информацию по количеству аренд за этот месяц.

**Процесс выполнения:**

```
WITH MonthlyPayments AS (
    SELECT
        DATE_FORMAT(payment_date, '%Y-%m') AS payment_month,
        SUM(amount) AS total_monthly_amount,
        COUNT(rental_id) AS total_monthly_rentals
    FROM
        payment
    GROUP BY
        payment_month
)
SELECT
    mp.payment_month,
    mp.total_monthly_amount,
    mp.total_monthly_rentals
FROM
    MonthlyPayments mp
ORDER BY
    mp.total_monthly_amount DESC
LIMIT 1;
```

![Сумма платежей и количество аренд](img/7.png)
